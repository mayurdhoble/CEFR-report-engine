# ── Local development ──────────────────────────────────────────────────────────
BASE_URL=http://localhost:8000
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/cefr_db

# ── Railway (set in Railway dashboard, not here) ───────────────────────────────
# DATABASE_URL  → auto-injected by Railway PostgreSQL plugin
# BASE_URL      → your backend Railway public URL
#                 e.g. https://cefr-backend-production.up.railway.app
# PORT          → auto-injected by Railway (do not set manually)
